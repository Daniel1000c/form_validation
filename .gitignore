# Node modules (if you're using npm or yarn)
node_modules/

# Build output (like from Webpack, Vite, Parcel, etc.)
dist/
build/
out/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS-specific
.DS_Store       # macOS
Thumbs.db       # Windows

# Editor-specific
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Dependency lock files (optional, depending on your setup)
# Uncomment if you don't want to commit lock files
# package-lock.json
# yarn.lock

# Environment variable files (if you're using them)
.env
.env.local
.env.*.local

# Misc
coverage/
.cache/
*.tmp
*.swp

# Optional: ignore compiled CSS/JS if you're using preprocessors (e.g., SCSS, TypeScript)
# Remove if you want to track compiled files
*.css.map
*.js.map

# Optional: ignore any zip or tar files
*.zip
*.tar
*.tgz